package routes

import (
	"eltimn/todo-plus/models"
	"eltimn/todo-plus/web/layouts"
)

templ ErrorPartial(err error) {
	<div id="error_container" class="error">Error: { err.Error() }</div>
}

templ ErrorPage(user *models.User, err error, nonce string) {
	@layouts.TailwindUILayout(user, nonce) {
		@ErrorPartial(err)
	}
}
